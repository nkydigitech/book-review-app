# WARNING: Do NOT commit real credentials here.
# The Azure DevOps pipeline substitutes these values at deploy time
# using pipeline secret variables.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: book-review
type: Opaque
stringData:
  DB_USER: "$(DB_USER)"
  DB_PASSWORD: "$(DB_PASSWORD)"
